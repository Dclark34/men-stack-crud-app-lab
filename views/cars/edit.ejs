<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Car</title>
</head>

<body>
    <h1>Edit <%= cars.model %></h1>
    <form action="/cars/<%= cars._id %>?_method=PUT" method="POST">
        <div>
            <label for="model">Model: </label>
            <input type="text" name="model" id="model" maxlength="25" value="<%= cars.model %>" required />
        </div>
        <div>
            <label for="year">Year: </label>
            <input type="text" name="year" id="year" maxlength="4" value="<%= cars.year %>" required/>
        </div>
        <div>
            <label for="color">Color: </label>
            <input type="text" name="color" id="color" maxlength="20" value="<%= cars.color %>" required/>
        </div>
        <div>
            <label for = "isUsed">Pre-owned: </label>
            <input type = "checkbox" name ="isUsed" id = "isUsed" <% if (cars.isUsed) { %>checked <%}%>/>
        </div>
            <label for="description">Description: </label>
            <textarea type="text" name="description" id="description" value="<%= cars.description %>" maxlength="250"><%= cars.description %></textarea>
        <div>
        <button type="submit">Update Entry</button>
        </div>
    </form>
    <a href="/cars">Back to Cars</a>
</body>
</html>